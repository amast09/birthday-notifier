// import GoogleContactProvider from "./GoogleContactProvider";

describe("GoogleContactProvider", () => {
  describe("getContacts", () => {
    it("should retrieve the google contacts for the passed in Google account", () => {
        // const mockGoogle = jest.fn();
        // const googleContactProvider = GoogleContactProvider(mockGoogle);
        //
        // expect()
    });
  });
});
